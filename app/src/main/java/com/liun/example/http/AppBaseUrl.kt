package com.liun.example.http

/**
 * Description:
 * Author：Liun
 * Date:2019/01/02 16:58
 *
 */
object AppBaseUrl {
    val BaseUrl = "https://www.wanandroid.com/"

    val loginUrl = BaseUrl + "user/login"
    val bannerList = BaseUrl + "banner/json"
    val projectTree = BaseUrl + "project/tree/json"
    val projectList = BaseUrl + "project/list"
    val wxarticleChapters = BaseUrl + "wxarticle/chapters/json"
    val wxarticleList = BaseUrl + "wxarticle/list"
    val treeList = BaseUrl + "tree/json"
    val articleList = BaseUrl + "article/list"
    val logout = BaseUrl + "user/logout/json"

}